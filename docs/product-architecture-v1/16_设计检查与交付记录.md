# 16 设计检查与交付记录

日期：2026-09-03。交付范围：成品设计文件和本地开发任务；没有修改业务代码，没有执行数据库迁移，没有提交或推送 GitHub。

## 已完成的设计检查

```bash
python3 docs/product-architecture-v1/validate_design.py
node node_modules/typescript/bin/tsc --noEmit --strict --skipLibCheck --target ES2022 --module NodeNext --moduleResolution NodeNext docs/product-architecture-v1/contracts/domain.ts
```

两条命令均通过。检查覆盖：

- 文档与 CCPM 文件的本地链接可解析。
- OpenAPI JSON 可读取，内部引用、路径参数和 operationId 一致。
- 40 个领域/HTTP schema、六个工具参数 schema 与 parser manifest 的 JSON Schema 结构合法。
- 核心 Run/Phase/Outcome 枚举与 TypeScript 一致，领域类型可以单独编译。
- 两轮示例通过状态、草稿、事实和证据 schema；引用对象存在。
- 示例毛利率重算、预测未到期处理、同一观点 ID、用户判断继承和问题解决符合约定。
- 数据库 DDL 的表引用静态存在，九个开发任务依赖无环且都可定位。

## 不把这些检查误解成什么

这些是设计一致性检查，不是产品运行测试。当前环境未执行 PostgreSQL DDL，也未运行专门的 OpenAPI 全规范验证器；JSON Schema 校验不能替代这两项。真实官方财报获取、中文 PDF 提取、模型输出质量、浏览器交互与部署恢复，都列在实现任务的成品验收中，尚未由本次文档工作证明。

## 设计复核中已补齐的连接点

1. 加入 evidence-bundle 接口，确保页面不仅拿到事实 ID，还能取得对应数字、计算和来源。
2. 把草稿失效、重算、确认和并发 rebase 接到同一状态机。
3. 记录 target-gap 计算的观点标准来源，支持后续解释计算用了哪个目标。
4. 保留财报获取失败与“没有新披露”的区别。
5. 将每条 P0 映射到实现任务与验收，不留下只有产品描述、无人负责实现的项目。
6. 用 CCPM 拆成九个可执行任务，写明依赖、文件所有权和实际验证要求；没有创建外部 issue。

## 下一步

架构负责人审阅这套终态设计后，先把任务 001 分配给 Luna max，并尽早安排 002 的真实 PDF 小测、003 的官方资料获取小测。前端 006 在契约稳定后并行。任务 009 完成真实两轮验收，才将整个 Epic 标记为完成。
