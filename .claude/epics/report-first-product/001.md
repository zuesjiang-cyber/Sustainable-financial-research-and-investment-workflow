---
name: 领域契约、PostgreSQL 与持久任务底座
status: open
created: 2026-09-03T12:39:08Z
updated: 2026-09-03T12:39:08Z
depends_on: []
parallel: false
conflicts_with: ["008"]
---

# 001 领域契约、PostgreSQL 与持久任务底座

## 任务目标

实现领域 Zod schema 与 API DTO；建立目标表和复合外键；把 run/job/checkpoint/draft 持久化；本地 StorageAdapter；幂等键与工作区上下文；供后续模块调用的 Repo 接口。

## 必读

[成品设计入口](../../../docs/product-architecture-v1/00_README.md)，重点阅读 02、03、08、09 和 contracts。

## 文件所有权

src/shared/**、src/server/repos/**、src/server/jobs/{queue,lease,checkpoint}*、src/server/storage/**、db/migrations/**；共享 app.ts 接入由本任务负责人统一处理。

## 验收

使用真实测试 PostgreSQL：创建 run 与 job 同事务；模拟租约过期与 Worker 重启；旧 lease token 不可提交；草稿不因 API 重启消失；领域类型和 OpenAPI 契约测试通过。

执行相关类型/契约/集成测试并附实际命令与输出摘要。测试命令由任务 001 建立统一命名；未存在的命令先补实现，不能仅在报告中声称运行。

## 交付说明

保留当前 SQL.js 数据，暂不迁移或清空。先落小型纵向贯通底座，不一次重写所有旧模块。

向架构负责人交付：修改文件、接口与数据影响、已运行测试、真实操作步骤、未完成项。模型配置按用户要求使用 gpt-5.6-luna / max；这个任务文件本身不自动启动 Agent。
