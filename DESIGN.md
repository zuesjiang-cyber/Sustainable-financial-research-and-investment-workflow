# FinTrust V1 亮色研究工作台

## 视觉原则

- 以 `#F6F8FC` 作为页面底色、白色作为信息卡片表面，保持机构级研究工具的安静层级。
- 主文字使用深海军蓝，主行动使用钴蓝；成功使用绿色，待跟踪使用琥珀色，削弱/风险使用红色。
- 使用现有系统字体，不引入联网字体或新的 npm 依赖。
- 卡片使用 8–12px 的克制圆角、细边框和轻阴影；避免紫色渐变、玻璃拟态和装饰性背景。

## 布局原则

- 首页采用 Demo-first：已核验 Demo 结果预览约占首屏 60–65%，真实研报上传约占 35–40%，两者同屏可见。
- 首页只突出“研报观点持续核验”这一 V1 入口，不展示旧的多轮跟踪、单次测试台或 SQLite Demo 模式切换。
- 真实流程保持：上传研报 → Ling-3.0-Flash-Fin 提炼观点 → 用户确认 T0 → 上传自己的官方财报 → 逐条核验并确认 T1/T2 → 本地 Markdown Research Memory 跨轮继承。
- Demo 是明确标注的静态只读 Research Draft / State：不创建项目、不调用 run、不上传财报、不写入真实 Memory。

## 信息层级

- 结果卡片始终按“旧观点 → 最新财报事实 → 差距 → 原因 → 下一步问题”呈现。
- 状态必须保留 `SUPPORTED`、`PARTIALLY_SUPPORTED`、`WEAKENED`、`UNRESOLVED` 的语义与颜色差异。
- 用户修正与确认拥有最终解释权；模型输出不能覆盖已确认的 Markdown Research Memory。
